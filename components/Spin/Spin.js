import React from "react";
import Loader from "react-loader-spinner";
import styles from "./Spin.module.scss";

const Spin = () => {
  return <div className={styles.spin_container}>Loading...</div>;
};

export default Spin;
